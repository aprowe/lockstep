

Export Dialog
- [x] Default to input file location, remember last changed path
- [x] Don't highlight the "Saved" button after its done
- [x] Button for setting path, but allow text input (editable path + browse button)
- [x] Add BPM to normalize to (target BPM input when normalize is checked)


Markers
- [x] Show number of markers selected
- [ ] Have BPM / beat count change the length of the region, but if the end is unlinked, just change the length of the Beat timeline 


Timeline
- [x] Selecting a region in the timeline should not change the view
- [x] Need different highlight for when top vs bottom markers selected
- [x] Selecting in between timelines should select on both timelines (lasso in WarpConnector)
- [ ] Nudge Context menu (modal) nudges markers by N frames — SKIPPED per user request


Regions
- [x] Detect / Tap BPM should be on the region level, so in the left panel

Features
- Snap upper region to tick marks
- Settings page
    - Auto play Files
    - new region
- Marker Flash
- Speed bar below

UI
[x] Unify Timelines
- Brighter ticks
- Play head on minimap
- Nav buttons together
- Next prev scene
- File bar compact button
- frame count
- Double click go back after region views


Bugs
- ctrl-a for select all in export panel
- Cut out segments < 1 frame long
- 0.5x -> 2.0x
- Play / pause issue
